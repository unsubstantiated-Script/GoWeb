{{template "base" .}}
{{define "content"}}
    <div class="container">
        <div class="row">
            <div class="col"><h1>About Page</h1></div>
            <p>{{index .StringMap "test"}}</p>
        </div>
    </div>
{{end}}